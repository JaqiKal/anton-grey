import React from "react";

const Footer = () => {
  return (
    <footer className=" text-slate-600 text-center text-xs">
      <p>{new Date().getFullYear()} Powered by Joy!</p>
    </footer>
  );
};

export default Footer;
